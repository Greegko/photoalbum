My application is a photoalbum locally stored images. Generate a component which reads the personal file system folder, reads all the fields, and one metadata file which stores few information. Display the images in a grid system.
